import 'package:flutter/material.dart';

class ClinicalHistoriesView extends StatelessWidget {
  const ClinicalHistoriesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Clinical Histories'),
    );
  }
}
